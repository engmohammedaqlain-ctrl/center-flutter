# تقرير الفروقات الشامل
## بين النسخة الأصلية `C:\Users\Mohammed06\center` والنسخة المحمولة `C:\Users\Mohammed06\center-mobile-app`

**تاريخ التدقيق:** 2026-09-09
**النسخة الأصلية:** React 18 + TypeScript + Vite + Dexie (IndexedDB) + Supabase + Capacitor/Tauri/Electron — `APP_VERSION = 1.2.2`
**النسخة المحمولة:** Flutter (Dart) — `version: 1.0.0+1`، تعلن `appVersion = '1.2.2'` في الكود

---

## 0. الخلاصة التنفيذية (اقرأ هذا أولاً)

النسخة المحمولة **ليست نفس التطبيق**. هي إعادة كتابة جزئية تغطي تقريباً **36%** من حجم الكود، وتغطي **الهيكل الظاهري** لخمس شاشات، لكنها تفقد:

| # | الخلل | الخطورة |
|---|-------|---------|
| 1 | **لا يوجد تخزين محلي إطلاقاً** — كل البيانات في الذاكرة `List<>` وتضيع عند إغلاق التطبيق | 🔴 قاتل |
| 2 | **بيانات وهمية مزروعة في الكود** (`_seed()`) تظهر لكل مستخدم عند كل تشغيل | 🔴 قاتل |
| 3 | **قاعدة عمل مخترعة**: إنشاء 4 أقساط ودَين `الرسم × 4` تلقائياً لكل طالب جديد — غير موجودة في الأصل | 🔴 قاتل |
| 4 | **حذف الطالب يمحو سنداته المالية** بلا أي مانع — الأصل يمنع الحذف إن وُجد سند غير ملغى | 🔴 قاتل |
| 5 | **قسم الجداول والمجموعات (Schedule) مفقود بالكامل** — 8 ملفات، ~1,700 سطر | 🔴 عالٍ |
| 6 | **الصلاحيات تُحرَّر ولا تُطبَّق** — لا حراسة أقسام، لا إخفاء تبويبات، لا شاشة «لا تملك صلاحية» | 🔴 عالٍ |
| 7 | **الحضور ليوم واحد فقط** — الأصل شبكة أسبوع كامل (سبت→خميس) مع تنقّل بين الأسابيع | 🟠 عالٍ |
| 8 | **لا طباعة ولا PDF إطلاقاً** — السند، كشف الصف، الجدول، التقارير، الحضور | 🟠 عالٍ |
| 9 | **لا نسخ احتياطي حقيقي** — الزر يعرض إشعاراً فقط، ولا استرجاع | 🟠 عالٍ |
| 10 | **لا إعدادات مطور** — لا شعار، لا ألوان، لا اسم منشأة، لا نوع منشأة، لا تصفير قاعدة | 🟠 عالٍ |
| 11 | **لا تحديث لحظي (Realtime)** — الأصل مشترك في قنوات Supabase لكل الجداول | 🟠 متوسط |
| 12 | **بوابة المطور محلية بالكامل** — المنشآت لا تُقرأ ولا تُكتب في السحابة | 🟠 متوسط |

**ما نُقل بجودة عالية:** محرك المزامنة (`sync.dart` ≈ 77% من `sync.ts`)، منطق الهاتف (`phone.dart`)، الألوان والخط، نموذج الطالب الكامل، منطق الدفعات والإلغاء والذمم.

---

## 1. مقارنة كمية

| المقياس | الأصلي (center) | المحمول (center-mobile-app) | الفارق |
|---------|-----------------|------------------------------|--------|
| ملفات المصدر | 82 ملف `.ts/.tsx` | 21 ملف `.dart` | −61 |
| أسطر الكود | **24,547** | **8,835** | **−15,712 (−64%)** |
| ملفات الاختبار | 4 (~1,530 سطر) | 3 (162 سطر) | −1,368 |
| جداول التخزين المحلي | 18 جدول Dexie/IndexedDB | **0** (ذاكرة فقط) | −18 |
| نماذج بيانات (Models) | 20 واجهة | 12 صنف | −8 |
| شاشات/صفحات | 9 صفحات + 4 لوحات إعدادات + قسم جدول كامل | 11 شاشة | — |

---

## 2. خريطة المقابلات بين الملفات

### 2.1 ما له مقابل

| الأصلي | المحمول | التغطية |
|--------|---------|---------|
| `lib/sync.ts` (1157) | `lib/data/sync.dart` (887) | ~77% — ينقص Realtime و`syncAll` |
| `lib/phoneUtils.ts` (98) | `lib/data/phone.dart` (92) | ~100% |
| `lib/permissions.ts` (279) | `lib/data/permissions.dart` (83) | ~30% |
| `types/*.ts` (228) | `lib/models/models.dart` (1031) | ~70% من الكيانات |
| `pages/Students.tsx` (353) | `screens/students_screen.dart` (202) | ~75% |
| `pages/StudentDetail.tsx` (1421) | `screens/student_detail_screen.dart` (402) | ~40% |
| `features/students/StudentForm.tsx` (1385) | `screens/student_form_screen.dart` (973) | ~85% |
| `pages/Finance.tsx` (841) | `screens/finance_screen.dart` (326) | ~55% |
| `features/finance/PaymentForm.tsx` (539) | `screens/payment_form_screen.dart` (338) | ~85% |
| `features/finance/ReceiptModal.tsx` (313) | `screens/receipt_screen.dart` (129) | ~45% |
| `pages/Attendance.tsx` (1029) | `screens/attendance_screen.dart` (205) | ~25% |
| `pages/SchoolClasses.tsx` (961) | `screens/classes_screen.dart` (460) | ~65% |
| `pages/Settings.tsx` + 8 لوحات (~3,900) | `screens/settings_screen.dart` (1177) | ~30% |
| `pages/DeveloperDashboardPage.tsx` (989) | `screens/developer_screen.dart` (196) | ~20% |
| `pages/Reports.tsx` (604) | `screens/reports_screen.dart` (141) | ~25% |
| `pages/LandingPage.tsx` (295) | `LoginScreen` داخل `main.dart` | ~40% |
| `components/mobile/*` + `layout/*` | `screens/shell.dart` + `widgets/widgets.dart` | ~70% |

### 2.2 ما لا مقابل له إطلاقاً (مفقود 100%)

| الملف الأصلي | الأسطر | الوظيفة |
|--------------|--------|---------|
| `lib/db.ts` | 155 | قاعدة Dexie/IndexedDB — 18 جدول، 3 إصدارات ترحيل |
| `pages/Schedule.tsx` | 475 | شاشة الجداول والمجموعات |
| `features/schedule/schedule.service.ts` | 260 | إنشاء/تعديل/حذف مجموعة، تسجيل طالب، فحص التعارضات |
| `features/schedule/GroupForm.tsx` | 280 | نموذج المجموعة الدراسية |
| `features/schedule/EnrollmentModal.tsx` | 386 | نافذة تسجيل الطلاب في المجموعات |
| `features/schedule/ScheduleTable.tsx` | 146 | جدول المجموعات |
| `features/schedule/WeeklyScheduleView.tsx` | 124 | عرض الجدول الأسبوعي |
| `features/schedule/SchedulePDFModal.tsx` | 273 | تصدير الجدول PDF |
| `features/schedule/schedule.utils.ts` | 27 | أدوات مساعدة للجدول |
| `features/attendance/attendance.service.ts` | 174 | إدارة الجلسات (sessions) وحفظ كشوف الحضور |
| `features/settings/DeveloperSettings.tsx` | 937 | هوية المنشأة، الشعار، الألوان، البيانات التجريبية، تصفير القاعدة |
| `features/settings/BackupSettings.tsx` | 333 | تصدير/استيراد نسخة احتياطية فعلية |
| `features/settings/Rooms.tsx` | 473 | إدارة القاعات (وضع «مركز») |
| `features/settings/CapabilityEditor.tsx` | 110 | محرر الصلاحيات مع الاعتماديات |
| `features/settings/FailedSyncPanel.tsx` | 153 | لوحة العمليات المتعثرة مع إعادة المحاولة |
| `features/classes/ClassPrintRoster.tsx` | 152 | كشف الصف للطباعة |
| `lib/institution.ts` | 591 | هوية المنشأة، الألوان، الشعار، اقتباس الألوان من الشعار، الترخيص |
| `lib/tenantService.ts` | 332 | إدارة المنشآت عبر Supabase، التحقق من الاشتراك |
| `lib/demoData.ts` | 506 | مولّد البيانات التجريبية (باستدعاء صريح فقط) |
| `lib/currentUser.ts` | 125 | هوية الجهاز، المستخدم الحالي، اسم المستلم على السند |
| `lib/session.ts` | 56 | إدارة الجلسة |
| `hooks/useRealtimeListener.ts` | 47 | الاستماع للتحديثات اللحظية |
| `hooks/usePermissions.ts` | 64 | Hook الصلاحيات |
| `components/tenant/NewDeviceSetupModal.tsx` | 290 | تهيئة جهاز جديد + سحب أولي + تحديد الصلاحية |
| `components/tenant/SubscriptionStatusBanner.tsx` | 37 | شريط تحذير انتهاء الاشتراك |
| `components/ErrorBoundary.tsx` | 103 | التقاط الأخطاء غير المتوقعة |
| `components/layout/Sidebar.tsx` / `Header.tsx` | 661 | واجهة سطح المكتب |
| `lib/useViewMode.ts` | 77 | التبديل بين واجهة الهاتف وسطح المكتب |
| **المجموع المفقود** | **≈ 8,470 سطر** | |

---

## 3. الميزات المفقودة كلياً — بالتفصيل

### 3.1 🔴 قسم الجداول والمجموعات (Schedule) — مفقود بالكامل

الأصل يدعم **نوعين من المنشآت**:
- `institution_type = 'school'` → تبويب **«الصفوف»** (`SchoolClasses`)
- `institution_type = 'center'` → تبويب **«الجدول»** (`Schedule`)

النسخة المحمولة **مثبّتة على وضع المدرسة فقط**. كل ما يلي غير موجود:

- **`Group`** (المجموعة الدراسية): الاسم، المادة، المدرّس، القاعة، المرحلة، السعر الشهري، الحد الأقصى للطلاب، أيام الأسبوع، وقت البدء والانتهاء، الحالة
- **`StudentEnrollment`** (تسجيل الطالب في مجموعة): السعر المخصص، سبب الخصم، السعر المطبَّق المثبَّت وقت التسجيل، حالة التسجيل (نشط/منسحب/مكتمل/موقوف)
- **إنشاء وتعديل وحذف المجموعات** (`GroupForm`)
- **تسجيل الطلاب في المجموعات وإلغاء تسجيلهم** (`EnrollmentModal`)
- **فحص تعارضات الجدول** (`ScheduleService.checkConflicts`) — تعارض المدرّس أو القاعة في نفس الوقت
- **العرض الأسبوعي للجدول** (`WeeklyScheduleView`)
- **تصدير الجدول PDF** (`SchedulePDFModal`)
- `DAYS_OF_WEEK` (أيام الأسبوع) المعرّفة في `shared/constants.ts`

> ⚠️ جدول `groups` و`enrollments` موجودان في قائمة المزامنة `syncedTables` في Flutter، لكن كصفوف خام في `extraCloud` — تُرفع وتُسحب ولا تُقرأ ولا تُعرض ولا تُستعمل في أي شاشة.

### 3.2 🔴 التخزين المحلي — غائب تماماً

**الأصل:** `lib/db.ts` — قاعدة Dexie فوق IndexedDB بـ **18 جدول** و**3 إصدارات مخطط** مع ترحيل:

```
students, attachments, enrollments, payments, teacherPayouts, expenses,
cashboxShifts, sessions, attendance, users, teachers, subjects, rooms,
groups, gradeFees, installments, institutionSettings, pendingSyncs
```
مع فهارس على `national_id, phone, status, grade_level, sync_status, receipt_number, session_date...`
وترحيل v3 يفصل المرفقات (Base64) عن سجل الطالب لأن تحميلها مع كل قائمة كان يستنزف الذاكرة.

**المحمول:** قوائم في الذاكرة داخل `AppStore`:
```dart
final teachers = <Teacher>[];   final students = <Student>[];
final subjects = <SubjectItem>[]; final payments = <Payment>[];
final rooms = <Classroom>[];    final installments = <Installment>[];
final attendance = <AttendanceMark>[]; final gradeFees = <GradeFee>[];
final users = <AppUser>[];      final tenants = <Tenant>[];
```

`SharedPreferences` مستعملة في موضع واحد فقط: `last_pull_at_$tenantId` في [sync.dart:454](lib/data/sync.dart#L454).

**النتيجة العملية:** إغلاق التطبيق = ضياع كل شيء لم يُرفع للسحابة. طابور المزامنة `pendingSyncs` نفسه في الذاكرة ويضيع معه.

### 3.3 🔴 البيانات الوهمية المزروعة في الكود

`AppStore._seed()` في [store.dart:791](lib/data/store.dart#L791) يزرع عند كل إقلاع:
- منشأتين وهميتين: `مدرسة الأمل الخاصة (amal/amal2026)` و`مدرسة النور (noor/noor2026)`
- مستخدمين: `أحمد عبد الله` (مدير)، `سارة خالد` (سكرتير)
- 5 مدرّسين، 5 مواد، صفوف، رسوم، وطلاب

في الأصل هذه البيانات موجودة في `lib/demoData.ts` وتُحقن **بضغطة زر صريحة** من إعدادات المطور، ويقابلها **زر تصفير القاعدة**.

**كذلك:** بيانات دخول المدرسة مثبّتة في الكود:
```dart
String _tenantUser = 'amal';
String _tenantPass = 'amal2026';
```

### 3.4 🔴 نظام الصلاحيات مُعطَّل عملياً

| العنصر | الأصل | المحمول |
|--------|-------|---------|
| قائمة الـ 19 صلاحية | ✅ | ✅ مطابقة حرفياً |
| مجموعات العرض `CAPABILITY_GROUPS` | ✅ | ✅ مطابقة |
| قوالب الأدوار (مدير/سكرتير) | ✅ | ✅ مطابقة |
| `CAPABILITY_REQUIRES` (الاعتماديات) | ✅ 11 علاقة | ❌ مفقود |
| `CAPABILITY_DEPENDENTS` (المعكوس) | ✅ | ❌ مفقود |
| `toggleCapability` / `toggleCapabilityGroup` | ✅ دوال صرفة مختبَرة | ❌ مفقود |
| `userCan()` + احترام `is_active` | ✅ | ❌ مفقود |
| `SECTION_CAPABILITY` (حراسة الأقسام) | ✅ | ❌ مفقود |
| `SchoolRoute` — منع الوصول بالمسار المباشر | ✅ | ❌ مفقود |
| شاشة «قسم … غير متاح لصلاحيتك» | ✅ | ❌ مفقود |
| إخفاء تبويبات الإعدادات حسب الصلاحية | ✅ | ❌ مفقود |
| `usePermissions` hook | ✅ | ❌ مفقود |

الاستعمال الوحيد في Flutter: `effectiveCapabilities()` داخل محرر الصلاحيات في [settings_screen.dart:930](lib/screens/settings_screen.dart#L930) — أي أن المدير يحدد صلاحيات **لا تُقرأ في أي مكان آخر**.

**النتيجة:** كل مستخدم يرى كل شيء.

### 3.5 🟠 الطباعة و PDF — غائبة كلياً

`pubspec.yaml` لا يحتوي على أي حزمة طباعة (`printing` / `pdf`). المفقود:

| الشاشة | ما يفعله الأصل |
|--------|----------------|
| سند القبض | `window.print()` + «تنزيل PDF» + ترويسة المنشأة والشعار |
| كشف الصف | `ClassPrintRoster` — كشف طلاب الصف مرقّم مع خانات توقيع |
| كشف الحضور | زر طباعة الشبكة الأسبوعية |
| الجدول الأسبوعي | `SchedulePDFModal` |
| التقارير | `handlePrint()` لتقرير المركز وكشف حساب الطالب |

### 3.6 🟠 النسخ الاحتياطي والاسترجاع

**الأصل** (`BackupSettings.tsx`):
- `handleExportBackup` — تصدير كل الجداول إلى ملف JSON قابل للتنزيل
- `handleImportBackup` — قراءة الملف والتحقق منه واستعادة البيانات
- إحصائيات حية بعدد السجلات لكل جدول + زر تحديث
- بوابة الدخول لإعدادات المطور

**المحمول** ([settings_screen.dart:1154](lib/screens/settings_screen.dart#L1154)):
```dart
PrimaryButton(
  label: 'تصدير نسخة احتياطية',
  onPressed: () => showAppSnack(context, 'تم تجهيز النسخة الاحتياطية محلياً (...)'),
)
```
**لا يُنتج أي ملف.** ولا يوجد زر استرجاع إطلاقاً.

### 3.7 🟠 إعدادات المطور وهوية المنشأة

`DeveloperSettings.tsx` (937 سطر) + `institution.ts` (591 سطر) — **صفر مقابل** في Flutter:

- قفل بكلمة مرور المطور قبل الدخول
- **نوع المنشأة**: مدرسة ↔ مركز تعليمي (يغيّر التبويبات والشاشات)
- **اسم المنشأة** والترويسة الرسمية
- **رفع الشعار** (Base64) وإزالته
- **ألوان الهوية**: خلفية القائمة، لون التمييز، زر أساسي، زر إجراء، خلفية العمل
- **`extractColorsFromLogo`** — اقتباس لوحة الألوان تلقائياً من الشعار
- **`COLOR_PRESETS`** — تشكيلات لونية جاهزة
- **معاينة حية** للسند والقائمة الجانبية قبل الحفظ
- **`applyThemeColors`** — كتابة متغيرات CSS ديناميكياً
- **`persistInstitutionSettings` / `hydrateInstitutionSettings`** — حفظ الهوية في جدول `institution_settings` ومزامنتها لكل الأجهزة
- **حقن البيانات التجريبية** / **تصفير قاعدة البيانات**
- **معلومات النظام**: إصدار المنظومة، القاعدة المحلية، وضع التخزين
- **`LicenseConfig`** — إعدادات الترخيص

في Flutter: `institutionName` متغيّر نصي واحد في `AppStore`، والألوان ثوابت في `AppColors`.

### 3.8 🟠 التحديث اللحظي (Realtime)

الأصل يشترك في قنوات Supabase Realtime لكل الجداول الـ17 ([sync.ts:647](../center/src/renderer/lib/sync.ts)) ويبثّ حدث `center_db_updated`، وكل شاشة تستمع عبر `useRealtimeListener`:

```
Students      → ['students', 'enrollments']
StudentDetail → ['students','enrollments','payments','installments','attendance']
Finance       → ['payments','students','installments','expenses','teacher_payouts','cashbox_shifts']
Attendance    → ['attendance','sessions','groups','rooms','students','enrollments']
SchoolClasses → ['rooms','teachers','students','grade_fees']
```

Flutter: لا اشتراك Realtime. التحديث يدوي فقط بزر «سحب».

### 3.9 🟠 تهيئة الجهاز الجديد وحالة الاشتراك

- `NewDeviceSetupModal` (290 سطر): عند أول تشغيل على جهاز فارغ — إدخال كود المنشأة، سحب أولي كامل من السحابة، تحديد صلاحية الجهاز، وعلامة `initial_setup_done_<tenantId>`
- `SubscriptionStatusBanner`: شريط تحذير عند انتهاء أو إيقاف الاشتراك مع رابط بوابة المطور
- `ErrorBoundary`: التقاط الأخطاء غير المتوقعة بدل الشاشة البيضاء

**كلها مفقودة.**

---

## 4. الفروقات شاشة بشاشة

### 4.1 شاشة الطلاب — `Students.tsx` ↔ `students_screen.dart`

| العنصر | الأصل | المحمول |
|--------|-------|---------|
| البحث بالاسم/الهاتف/الهوية/ولي الأمر | ✅ | ✅ |
| تصفية المرحلة | ✅ | ✅ |
| عدّاد `المعروض / الإجمالي` | ✅ | ✅ |
| بطاقة الطالب + شارة الرصيد | ✅ | ✅ |
| اتصال + واتساب مباشر | ✅ | ✅ |
| **الفرز بالأحدث (`created_at` تنازلياً)** | ✅ | ❌ |
| **تعديل الطالب من القائمة** | ✅ `onEdit` | ❌ (من الملف فقط) |
| **حذف الطالب من القائمة + نافذة تأكيد** | ✅ | ❌ |
| **زر مسح البحث (X)** | ✅ | ❌ |
| **حالة التحميل** | ✅ | ❌ |

### 4.2 ملف الطالب — `StudentDetail.tsx` (1421) ↔ `student_detail_screen.dart` (402)

| العنصر | الأصل | المحمول |
|--------|-------|---------|
| بطاقة الرصيد + إجمالي المقبوضات | ✅ | ✅ |
| بيانات التواصل + اتصال/واتساب | ✅ | ✅ |
| الأقساط المجدولة + تسديد قسط | ✅ | ✅ |
| سجل الدفعات + فتح السند | ✅ | ✅ |
| حذف الطالب | ✅ | ✅ |
| تقييم النجوم الخمس | ✅ | ✅ |
| **إرسال السند بالواتساب برسالة مفصّلة** (رقم السند، المبلغ، الطريقة، الغرض، المحوّل، المرجع، التاريخ، المتبقي) | ✅ | ❌ |
| **عرض المرفقات** (هوية الطالب، هوية ولي الأمر، شهادة الميلاد) + تكبير الصورة | ✅ | ❌ (تُرفع في النموذج ولا تُعرض) |
| **المجموعات المسجّل بها** + إلغاء التسجيل | ✅ | ❌ |
| **تسجيل الطالب في مجموعة** (سعر مخصص + سبب الخصم) | ✅ | ❌ |
| **سجل الحضور التفصيلي بالتواريخ** + «عرض الكل» | ✅ | ❌ (عدّادات فقط) |
| **إحصاء المتأخر والمعذور** (`late` / `excused`) | ✅ | ❌ |
| **اتصال/واتساب منفصل لولي الأمر** | ✅ | جزئي |

### 4.3 المالية — `Finance.tsx` (841) ↔ `finance_screen.dart` (326)

| العنصر | الأصل | المحمول |
|--------|-------|---------|
| تبويب المقبوضات + تبويب المستحقات | ✅ | ✅ |
| فلترة: طريقة الدفع، الحالة (مقبوضة/ملغاة) | ✅ | ✅ |
| فلترة المستحقات: الكل/مستحق/متأخر/استثناء | ✅ | ✅ |
| إجمالي المستحقات | ✅ | ✅ |
| تسديد سريع من صف المستحق | ✅ | ✅ |
| **البحث في `notes`، `sender_name`، `transfer_channel`** | ✅ | ❌ (رقم السند + الاسم + المرجع فقط) |
| **بطاقات إحصائية**: عدد المتأخر، عدد المستحق، عدد الاستثناءات | ✅ | ❌ |
| **إلغاء السند من قائمة المقبوضات** | ✅ | ❌ (من داخل السند فقط) |
| **رابط `?tab=dues`** للانتقال المباشر | ✅ | ❌ |
| **المصروفات (`Expense`)** | ✅ نموذج + مزامنة | ❌ لا نموذج |
| **رواتب المدرّسين (`TeacherPayout`)** | ✅ | ❌ |
| **ورديات الصندوق (`CashboxShift`)** | ✅ | ❌ |

### 4.4 الحضور — `Attendance.tsx` (1029) ↔ `attendance_screen.dart` (205)

**هذا أكبر تراجع وظيفي في التطبيق.**

| العنصر | الأصل | المحمول |
|--------|-------|---------|
| اختيار المرحلة ثم الشعبة | ✅ | ✅ |
| رصد حاضر/غائب | ✅ | ✅ |
| «الكل حاضر» | ✅ | ✅ |
| عدّاد حاضر/غائب لليوم | ✅ | ✅ |
| **شبكة الأسبوع المدرسي (سبت→خميس، 6 أيام)** | ✅ | ❌ **اليوم الحالي فقط** |
| **التنقل بين الأسابيع (`weekOffset`)** | ✅ | ❌ |
| **عدد غيابات الطالب خلال الأسبوع** | ✅ | ❌ |
| **دورة النقر الثلاثية** (غير مرصود → غائب → حاضر → غير مرصود) | ✅ | ❌ (تبديل ثنائي) |
| **الحالات `late` و `excused`** | ✅ في `AttendanceStatus` | ❌ |
| **سجلات الجلسات (`ClassSession`)** مع `getOrCreateSession` | ✅ | ❌ (المفتاح `att-{studentId}-{date}` فقط) |
| **حقل `marked_by_user_id` مملوء** | ✅ | ❌ (فارغ دائماً) |
| **وضع «مركز»**: الحضور بحسب المجموعة والتسجيلات | ✅ | ❌ |
| **زر الطباعة** | ✅ | ❌ |
| **`ensureSessionsForDate`، `saveSessionAttendance`، `updateSessionStatus`، `getRecentAbsences`** | ✅ | ❌ |

### 4.5 الصفوف والشعب — `SchoolClasses.tsx` (961) ↔ `classes_screen.dart` (460)

| العنصر | الأصل | المحمول |
|--------|-------|---------|
| تصفية بالمرحلة التعليمية الكبرى + عدّادات | ✅ | ✅ |
| بطاقة الصف (المربي، الطلاب/السعة، المدينون) | ✅ | ✅ |
| قائمة إجراءات (تعديل / تعيين مربي / حذف) | ✅ | ✅ |
| تفاصيل الصف + قائمة الطلاب + بحث | ✅ | ✅ |
| **طباعة كشف الصف** (`ClassPrintRoster`) | ✅ | ❌ |
| **ملاحظات الصف** | ✅ | ❌ |
| **تأكيد الحذف بنافذة صريحة** | ✅ | جزئي |
| **إشعار النجاح (`showNotification`)** | ✅ | ✅ عبر Snack |

### 4.6 نموذج الطالب — `StudentForm.tsx` (1385) ↔ `student_form_screen.dart` (973)

**أفضل جزء منقول.** كل الحقول الأساسية والموسعة موجودة:
الاسم الرباعي، الهوية، المرحلة، الشعبة، هاتف الطالب بالمقدمة، ولي الأمر وصلة القرابة وهاتفه، الحي (23 حياً)، العنوان المفصل، تاريخ التسجيل، مصدر التعرف، الملاحظات، تاريخ الميلاد، الجنس، مكان الولادة، الجنسية، المدرسة السابقة، المعدل، السكن، المناطق الأصلية، الحالة الصحية ونوع المرض، مهنة ولي الأمر، الرقم البديل، البريد، المرفقات الثلاثة، التقييم، إقرار ولي الأمر.

**الفروقات:**
- ❌ الأصل يطبّق **رسم حجز المقعد** (`getSeatReservationFee`) على الرصيد عند `seat_reservation_paid` — Flutter لا
- ❌ Flutter لا يمرّر `payment_plan` / `payment_status` / `academic_discount_*` / `has_flexible_exception` من واجهة التعديل (تُحفظ افتراضيات فقط)
- ⚠️ الأصل **يمنع** حذف قسط عليه سند قبض غير ملغى؛ Flutter لا يحرّر الأقساط أصلاً

### 4.7 نموذج الدفعة — `PaymentForm.tsx` (539) ↔ `payment_form_screen.dart` (338)

مطابق تقريباً: بحث الطالب غير المسدد، ربط بقسط مجدول، المبلغ، الغرض (5 خيارات)، الغرض المخصص، طريقة الدفع (6)، التاريخ، جهة التحويل، اسم المحوّل، المرجع، تاريخ التحويل الفعلي، تفاصيل الوسيلة الأخرى، البيان.

**المفقود:** حقل **«المجموعة المرتبطة (اختياري)»** — `group_id` غير موجود في نموذج `Payment` في Flutter.

### 4.8 سند القبض — `ReceiptModal.tsx` (313) ↔ `receipt_screen.dart` (129)

| العنصر | الأصل | المحمول |
|--------|-------|---------|
| رقم السند، التاريخ، المبلغ، الطريقة، الغرض | ✅ | ✅ |
| المرجع، اسم المحوّل، البيان، المتبقي | ✅ | ✅ |
| إلغاء السند | ✅ | ✅ |
| **«وقدره كتابةً»** (المبلغ بالحروف العربية) | ✅ | ❌ |
| **شعار وترويسة المنشأة** | ✅ | ❌ (الاسم النصي فقط) |
| **جدول: المبلغ المسدد / المتبقي / الحالة** | ✅ | ❌ |
| **اسم المستلم** (`getReceiptReceiverLabel`) | ✅ | ❌ |
| **طباعة + تنزيل PDF** | ✅ | ❌ |
| **إرسال بالواتساب** | ✅ | ❌ |

### 4.9 الإعدادات — 9 ملفات (~3,900) ↔ `settings_screen.dart` (1177)

| التبويب | الأصل | المحمول |
|---------|-------|---------|
| المراحل الدراسية والرسوم | ✅ (552 سطر) | ✅ جيد |
| المدرسون | ✅ (505) | ✅ جيد |
| المواد الدراسية | ✅ (404) | ✅ جيد |
| المستخدمون والصلاحيات | ✅ (471) | ✅ جزئي |
| **القاعات** (وضع «مركز») | ✅ (473) | ❌ **مفقود** |
| النسخ الاحتياطي والبيانات | ✅ (333) | ⚠️ واجهة فقط |
| **إعدادات المطور** | ✅ (937) | ❌ **مفقود** |
| **لوحة العمليات المتعثرة + إعادة المحاولة** | ✅ (153) | ⚠️ عرض فقط |
| **رمز المنشأة في الشريط** | ✅ | ❌ |
| **إخفاء التبويبات حسب الصلاحية** | ✅ | ❌ |
| **قسم تسجيل الخروج المخصص** | ✅ | ❌ (من الترويسة) |
| **`ensureOwnerAdmin`** — إنشاء حساب المالك تلقائياً | ✅ | ❌ |

### 4.10 بوابة المطور — `DeveloperDashboardPage.tsx` (989) ↔ `developer_screen.dart` (196)

| العنصر | الأصل | المحمول |
|--------|-------|---------|
| قائمة المنشآت | ✅ | ✅ |
| إضافة منشأة | ✅ | ✅ |
| تمديد الاشتراك | ✅ مرن (بالأشهر + معاينة التاريخ) | ⚠️ سنة واحدة ثابتة |
| حذف منشأة | ✅ بتأكيد كتابة الاسم | ⚠️ بلا تأكيد قوي |
| **القراءة والكتابة من Supabase** | ✅ | ❌ **محلي بالكامل** |
| **البحث في المنشآت** | ✅ | ❌ |
| **نوع الخطة** (محدد المدة / دائم) | ✅ | ❌ |
| **إيقاف / تفعيل الاشتراك** | ✅ | ❌ |
| **«دخول للمنشأة»** (تقمّص المنشأة) | ✅ | ❌ |
| **تعديل بيانات المنشأة inline** | ✅ | ⚠️ نموذج مبسّط |
| **اسم المالك، هاتفه، الملاحظات** | ✅ | ⚠️ الهاتف فقط |
| **توليد الكود تلقائياً من الاسم** | ✅ | ❌ |
| **عرض/نسخ اسم المستخدم وكلمة المرور** | ✅ | ⚠️ عرض جزئي |

### 4.11 التقارير — `Reports.tsx` (604) ↔ `reports_screen.dart` (141)

> ملاحظة: `Reports.tsx` **غير مربوط بأي مسار** في `App.tsx` بالنسخة الأصلية — كود يتيم. النسخة المحمولة ربطته بزر داخل تبويب البيانات.

| العنصر | الأصل | المحمول |
|--------|-------|---------|
| تقرير المركز / كشف حساب طالب | ✅ | ✅ |
| فترة: اليوم / هذا الشهر | ✅ | ✅ |
| **فترة مخصصة (من – إلى)** | ✅ | ❌ |
| **المصروفات ورواتب المدرّسين وصافي الدخل** | ✅ | ❌ |
| **نسبة الحضور للفترة** | ✅ | ❌ |
| **بحث الطالب** | ✅ | ❌ (قائمة منسدلة) |
| **مجموعات الطالب** | ✅ | ❌ |
| **الطباعة** | ✅ | ❌ |
| **شعار المنشأة في الترويسة** | ✅ | ❌ |

### 4.12 شاشة الدخول — `LandingPage.tsx` (295) ↔ `LoginScreen` في `main.dart`

| العنصر | الأصل | المحمول |
|--------|-------|---------|
| اسم مستخدم + كلمة مرور | ✅ | ✅ |
| حساب المطور `anas/anas2026` | ✅ | ✅ |
| **التحقق من المنشآت عبر Supabase** | ✅ | ❌ (قائمة محلية) |
| **`isSubscriptionValid`** (موقوف / منتهٍ) | ✅ | ⚠️ فحص مبسّط |
| **حفظ آخر اسم مستخدم** | ✅ | ❌ |
| **شاشة Splash + «معتمدة من وزارة التربية»** | ✅ | ❌ |
| **حفظ الجلسة بين التشغيلات** | ✅ localStorage | ❌ |
| **`custom_app_username/password` المحلية** | ✅ | ⚠️ متغيرات ذاكرة |

---

## 5. 🔴 اختلافات منطق العمل (الأخطر — سلوك مختلف لا نقص واجهة)

### 5.1 إنشاء أقساط ودَين تلقائي لكل طالب جديد — **قاعدة مخترعة**

[store.dart:355-380](lib/data/store.dart#L355):
```dart
if (isNew) {
  final fee = incoming.customMonthlyFee ?? feeFor(incoming.gradeLevel)?.monthlyFee ?? 200;
  incoming.balance = -(fee * 4);          // ← دَين تلقائي
  for (var p = 1; p <= 4; p++) {          // ← 4 أقساط تلقائية
    Installment(title: 'القسط الدراسي ($p)', amount: fee,
                dueDate: DateTime(today.year, today.month + (p - 1), 10), ...)
  }
}
```

**في الأصل:** `StudentForm.tsx` يحمل تعليقاً صريحاً في [السطر 510](../center/src/renderer/features/students/StudentForm.tsx):
> «الأقساط لا تُحرَّر من هذا النموذج ولا تُرسل معه. إرسالها فارغةً كان يُفهم على أنه أمر بحذفها كلها.»

و`StudentsService.create` ينشئ أقساطاً **فقط** إن مُرِّرت صراحةً — ولا شيء في التطبيق يمرّرها. الطالب الجديد يبدأ برصيد `0` (زائداً رسم حجز المقعد إن كان مدفوعاً).

**الأثر:** كل طالب يُسجَّل من الجوال يظهر فوراً بدَين وهمي وأربعة أقساط لم يوافق عليها أحد، وتُرفع إلى نفس قاعدة Supabase التي يستعملها التطبيق المكتبي.

### 5.2 حذف الطالب يمحو سنداته المالية

[store.dart:297](lib/data/store.dart#L297):
```dart
void deleteStudent(String id) {
  students.removeWhere((s) => s.id == id);
  payments.removeWhere((p) => p.studentId == id);   // ← يمحو السندات
  installments.removeWhere((i) => i.studentId == id);
  attendance.removeWhere((a) => a.studentId == id);
  ...
}
```

**في الأصل** `StudentsService.delete` **يرفض الحذف**:
```
لا يمكن حذف هذا الطالب لأن عليه N سند قبض مسجَّل.
يمكنك تغيير حالته إلى "منسحب" للاحتفاظ بالسجل المالي، أو إلغاء السندات أولاً.
```
والسندات الملغاة فقط تُحذف، وضمن معاملة ذرّية واحدة مع تسجيل عمليات `DELETE` في طابور المزامنة.

**الأثر:** فقدان سجل مالي دائم + عدم اتساق الإيرادات.

### 5.3 لا معاملات ذرّية (Atomic Transactions)

الأصل يغلّف `createPayment` و`cancelPayment` و`delete` في `db.transaction('rw', ...)` — إما تُكتب كل التغييرات أو لا شيء. تعليق صريح في الكود:
> «معاملة ذرّية: السند ورصيد الطالب والقسط يُكتبون معاً أو لا يُكتب أيٌّ منهم. كانوا ثلاث كتابات منفصلة، فأي انقطاع بينها يترك حالة مالية غير متسقة.»

Flutter: تعديلات متتابعة على قوائم في الذاكرة بلا أي ضمان.

### 5.4 توليد رقم السند لا يستشير السحابة

**الأصل** `generateReceiptNumber()`:
1. يحسب أعلى تسلسل محلي للسنة
2. **يستعلم Supabase** عن أعلى `receipt_number` لنفس `tenant_id` والسنة (آخر 200)
3. يأخذ الأكبر + 1
4. عند تعارض `UNIQUE(tenant_id, receipt_number)` يعيد التوليد

**المحمول** `_nextReceipt()`: محلي فقط.
**الأثر:** جهازان يُصدران نفس رقم السند → فشل الرفع.

### 5.5 لا ترحيل لأرقام السندات القديمة

`migrateReceiptNumbers()` في الأصل يوحّد `REC-2025-101` و`101` إلى `2025/1001`، مرة واحدة لكل جهاز خلف علامة `receipt_numbers_migrated_v1`، ويسجّل التغيير في طابور المزامنة. **مفقود في Flutter** — أرقام السندات القادمة من السحابة بصيغة قديمة تبقى كما هي.

### 5.6 حالة القسط الجزئي لا تُسجَّل

الأصل يضبط `status` إلى `'partially_paid'` عند سداد جزئي، و`'unpaid'` عند العكس. Flutter في [models.dart](lib/models/models.dart) `Installment.toCloud()`:
```dart
'status': isPaid ? 'paid' : status,   // status يبقى 'pending' الافتراضي
```
والقيمة الافتراضية `'pending'` **ليست من القيم المسموحة** في الأصل (`paid | unpaid | partially_paid`).

### 5.7 `received_by_user_id` و `marked_by_user_id` غير مملوءين

الأصل يستدعي `getCurrentUserId()` عند كل دفعة وكل رصد حضور. Flutter يترك الحقلين فارغين → لا يُعرف من قبض الدفعة ولا من رصد الغياب.

### 5.8 رسم حجز المقعد

الأصل: قيمة قابلة للتعديل من الإعدابات (`getSeatReservationFee`، افتراضي `0`) تُضاف للرصيد. تعليق صريح: «كان مثبّتاً بـ 50 في الكود — وهي قاعدة عمل مخترعة». Flutter: لا يطبّق الرسم إطلاقاً.

### 5.9 اختلاف تحديد «المتأخر» في الذمم

| | الأصل | المحمول |
|---|---|---|
| قسط له `due_date` | `due_date < today` | `dueDate < today` ✅ |
| قسط بلا تاريخ | `currentDay > 10` | — |
| دَين عام بلا أقساط | `currentDay > 10` | `DateTime.now().day > 10` ✅ |

متطابق عملياً، مع فارق أن الأصل يعالج القسط عديم التاريخ.

### 5.10 `guardian_relationship` مقابل `relation`

Flutter يخزّن الحقل باسم `relation` داخلياً ويوفّر getter/setter باسم `guardianRelationship`. يحتاج تدقيق أن `toCloud()` يكتب `guardian_relationship` — وهو ما يبدو صحيحاً، لكن أي حقل يخالف تسمية العمود يسقط في `sanitizePayload`.

---

## 6. فروقات نموذج البيانات

### 6.1 كيانات مفقودة كلياً

| الكيان | الأصل | المحمول |
|--------|-------|---------|
| `Group` (المجموعة الدراسية) | ✅ 13 حقلاً | ❌ صفوف خام في `extraCloud` |
| `StudentEnrollment` | ✅ 8 حقول | ❌ صفوف خام |
| `ClassSession` (جلسة) | ✅ 10 حقول | ❌ صفوف خام |
| `AttendanceRecord` | ✅ كامل | ⚠️ `AttendanceMark` مبسّط |
| `TeacherPayout` | ✅ 10 حقول | ❌ صفوف خام |
| `Expense` | ✅ 8 حقول | ❌ صفوف خام |
| `CashboxShift` | ✅ 10 حقول | ❌ صفوف خام |
| `InstitutionSettings` | ✅ 5 حقول | ❌ صفوف خام |
| `Subject` | ✅ | ✅ `SubjectItem` |
| `Room` | ✅ | ✅ `Classroom` |

### 6.2 حقول مفقودة في `Payment`

| الحقل | الأصل | المحمول |
|-------|-------|---------|
| `enrollment_id` | ✅ | ❌ |
| `group_id` | ✅ | ❌ |
| `received_by_user_id` | ✅ | ❌ |

### 6.3 حقول مفقودة في `AttendanceMark`

| الحقل | الأصل (`AttendanceRecord`) | المحمول |
|-------|---------------------------|---------|
| `status` بقيم `present\|absent\|excused\|late` | ✅ 4 قيم | ⚠️ `present\|absent` فقط |
| `session_id` مربوط بجلسة حقيقية | ✅ | ⚠️ فارغ غالباً |
| `marked_by_user_id` | ✅ مملوء | ⚠️ فارغ |

### 6.4 `Student` — تطابق شبه كامل ✅

كل الحقول الـ48 موجودة. الفارق الوحيد أن بعض الحقول المالية لا تُعدَّل من الواجهة.

### 6.5 `Tenant`

| الحقل | الأصل | المحمول |
|-------|-------|---------|
| `plan_type` (`rental\|lifetime`) | ✅ | ❌ |
| `status` (`active\|suspended`) | ✅ | ⚠️ `active` منطقي فقط |
| `notes` | ✅ | ❌ |
| `owner_name` | ✅ | ❌ |

---

## 7. البنية التحتية والمزامنة

### 7.1 محرك المزامنة — أقوى جزء منقول ✅

| العنصر | الأصل | المحمول |
|--------|-------|---------|
| `TABLE_ALLOWED_COLUMNS` | ✅ | ✅ مطابق |
| `NON_TEXT_COLUMNS` | ✅ | ✅ مطابق |
| `sanitizePayload` | ✅ | ✅ مطابق |
| `describeSupabaseError` (8 أنماط) | ✅ | ✅ مطابق |
| `queuePendingSync` + دمج العمليات | ✅ | ✅ |
| `MAX_SYNC_RETRIES = 5` | ✅ | ✅ |
| `STAMP_TOLERANCE_MS = 1000` | ✅ | ✅ |
| `getPendingSummary` / `checkRemoteChanges` | ✅ | ✅ |
| `push` / `pull` / `pushPendingChanges` / `pullFromCloud` | ✅ | ✅ |
| `getFailedActions` | ✅ | ✅ |
| `cleanupBogusDemoUserSyncs` | ✅ | ✅ |
| `lastPullAt` | ✅ localStorage | ✅ SharedPreferences |
| قائمة الجداول الـ17 | ✅ | ✅ مطابقة |
| **`setupRealtimeListeners`** | ✅ | ❌ |
| **`syncAll`** | ✅ | ❌ |
| **إعادة محاولة العمليات الفاشلة يدوياً** | ✅ `FailedSyncPanel` | ❌ عرض فقط |

**عنوان Supabase متطابق:** `https://tmybbunguiurisdcvrqo.supabase.co` — الجهازان يعملان على نفس القاعدة.

⚠️ **فارق أمني:** الأصل يقرأ العنوان والمفتاح من `.env` (`VITE_SUPABASE_URL` / `VITE_SUPABASE_ANON_KEY`) مع إمكانية تجاوزها من `localStorage`. Flutter يثبّتهما في [sync.dart:197-198](lib/data/sync.dart#L197) بلا أي إمكانية تغيير.

### 7.2 الهوية البصرية

| العنصر | الأصل | المحمول |
|--------|-------|---------|
| كحلي `#0B2545` | ✅ | ✅ |
| كهرماني `#E88C15` | ✅ | ✅ |
| خلفية `#F8FAFC`، حدود `#E2E8F0` | ✅ | ✅ |
| نجاح/خطر/معلومة | ✅ | ✅ |
| خط `IBM Plex Sans Arabic` | ✅ | ✅ عبر `google_fonts` |
| `borderRadius: 0` في كل شيء | ✅ | ✅ |
| `boxShadow: none` | ✅ | ✅ |
| **ألوان ديناميكية `var(--theme-*)`** | ✅ قابلة للتخصيص | ❌ ثوابت |
| RTL | ✅ | ✅ |

### 7.3 الاختبارات

| الأصل | أسطر | المحمول | أسطر |
|-------|------|---------|------|
| `finance.test.ts` | 867 | `store_test.dart` | 68 |
| `permissions.test.ts` | 410 | `sync_test.dart` | 80 |
| `schema.contract.test.ts` | 177 | `widget_test.dart` | 14 |
| `dateUtils.test.ts` | 78 | — | — |
| **المجموع** | **1,532** | **المجموع** | **162** |

`schema.contract.test.ts` يتحقق من تطابق أعمدة الكود مع مخطط Supabase — **لا مقابل له**، وهو أهم اختبار لمنع فشل الرفع.

---

## 8. أولويات الإصلاح المقترحة

### المرحلة 1 — إيقاف النزيف (لا يصلح للاستعمال قبلها)
1. **إضافة تخزين محلي دائم** (`sqflite` أو `drift`) بنفس مخطط Dexie الـ18 جدولاً، مع حفظ `pendingSyncs`
2. **حذف `_seed()`** ونقله خلف زر «حقن بيانات تجريبية» في إعدادات المطور
3. **حذف قاعدة إنشاء الأقساط الأربعة والدَين التلقائي** من `upsertStudent`
4. **إضافة مانع حذف الطالب** عند وجود سند قبض غير ملغى
5. **حفظ الجلسة** بين تشغيلات التطبيق

### المرحلة 2 — سلامة البيانات المالية
6. استشارة السحابة عند توليد رقم السند
7. ترحيل أرقام السندات القديمة (`migrateReceiptNumbers`)
8. ملء `received_by_user_id` و`marked_by_user_id`
9. ضبط حالة القسط `partially_paid` / `unpaid`
10. تطبيق رسم حجز المقعد

### المرحلة 3 — الصلاحيات والأمان
11. تطبيق `SECTION_CAPABILITY` على التبويبات وشاشة «لا تملك صلاحية»
12. نقل `CAPABILITY_REQUIRES` / `toggleCapability` مع الاعتماديات
13. نقل بيانات Supabase إلى إعداد قابل للتغيير

### المرحلة 4 — استكمال الوظائف
14. شبكة الحضور الأسبوعية + التنقل بين الأسابيع + `late`/`excused` + الجلسات
15. قسم الجداول والمجموعات كاملاً (`Group` + `Enrollment`)
16. المرفقات في ملف الطالب + إرسال السند بالواتساب
17. النسخ الاحتياطي والاسترجاع الفعليين
18. الطباعة و PDF (`printing` + `pdf`)
19. إعدادات المطور: نوع المنشأة، الاسم، الشعار، الألوان
20. بوابة المطور عبر Supabase
21. Realtime عبر قنوات Supabase
22. تبويب القاعات + وضع «مركز تعليمي»
23. المصروفات ورواتب المدرّسين وورديات الصندوق
24. `ErrorBoundary` + شريط حالة الاشتراك + تهيئة الجهاز الجديد

---

## 9. جدول الحكم النهائي

| القسم | نسبة المطابقة |
|-------|---------------|
| نموذج بيانات الطالب | 95% |
| نموذج تسجيل الطالب | 85% |
| نموذج الدفعة | 85% |
| محرك المزامنة | 77% |
| منطق الهاتف | 100% |
| الهوية البصرية | 90% |
| شاشة الطلاب | 75% |
| الصفوف والشعب | 65% |
| المالية | 55% |
| سند القبض | 45% |
| ملف الطالب | 40% |
| الإعدادات | 30% |
| الصلاحيات (تطبيقاً) | 5% |
| الحضور | 25% |
| التقارير | 25% |
| بوابة المطور | 20% |
| **الجداول والمجموعات** | **0%** |
| **التخزين المحلي** | **0%** |
| **الطباعة و PDF** | **0%** |
| **النسخ الاحتياطي** | **0%** |
| **إعدادات المطور والهوية** | **0%** |
| **التحديث اللحظي** | **0%** |
| **المتوسط المرجّح** | **≈ 36%** |
